[Unit]
Description=Dovecot Exporter
Wants=network-online.target
After=network-online.target

[Service]
User=root
Type=simple
ExecStart=/usr/local/bin/dovecot_exporter --web.listen-address=:{{ dovecot_exporter_port }}

[Install]
WantedBy=multi-user.target
